
spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/querydsl
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
#    properties:
#      hibernate:
#        format_sql: true
#
logging:
  level:
    org.spingframework.jdbc.core:
      JdbcTemplate: debug
      StatementCreatorUtils: trace
#    org.hibernate.SQL: debug